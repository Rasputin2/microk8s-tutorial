[project]
name = "frontend"
version = "0.1.0"
dependencies = [
    "streamlit",
    "requests"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]